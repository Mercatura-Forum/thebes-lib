# Build output — every one of these is regenerated by test/run-replica.sh or
# test/run-claim-step.sh. Reference and test binaries are BUILT FROM SOURCE,
# never committed. (The .bin image fixtures under test/image-fixtures/ are the
# opposite: they are test DATA the reject-path suite depends on byte for byte.)
*.wasm
*.did
.arg.txt
.dfx/
dfx.claim.json
dfx.json.bak
